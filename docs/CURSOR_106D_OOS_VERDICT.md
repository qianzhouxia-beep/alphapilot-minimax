# WorkBuddy → Cursor：选股模型准确率核查定论（106 维 OOS 有效）

日期: 2026-08-07
桥梁: 钱多爸已核准「WorkBuddy 负责评估/诊断，Cursor 负责 Qlib+PyTorch 落地」
关联: 服务器同步稿「选股模型准确率提升讨论」（W0 0.5993 / W1 0.6394 / W2 0.5330）

---

## 直接回答

**提纲里「W2 失效 0.533」是评估管线维度不足造成的假象，不是生产模型退化。**
**生产 106 维模型 OOS 有效且跨期稳定，撤回「状态感知 / 模型降权」类建议的前提。**

一句话：先对齐评估口径（维度 + 标签），再谈模型。口径对齐后，模型本身是好的。

---

## 铁证：维度链

同一套 walk-forward 框架，只换特征维度，标签统一为 1 日（FORWARD_DAYS=1，与 `train_v25.py` 对齐）：

| 维度 | W2 AUC | 说明 |
|------|--------|------|
| 67 维（旧管线 / 提纲原跑） | **0.533** | 资金流仅 2 只，侧车数据几乎没接 |
| 93 维 | 0.533 | 仍缺 chip / 侧车 |
| 101 维（本地 chip 2183 只旧） | 0.631 | 补侧车后跳升 |
| **106 维（生产维度，chip 4906 只完整）** | **0.631** | ✅ 生产模型口径 |
| 116 维（fd1 实验维度） | 0.675 | 非生产维度，仅供参考 |

**结论**：0.533 → 0.631 的跃升 100% 来自「补维 + 接 V3 侧车数据」，与模型权重无关。提纲的 0.533 是拿旧 67 维管线（且用了 5 日标签，与生产 1 日标签也不一致）去测生产模型，属于口径错配。

---

## 生产 106 维完整 OOS 结果（最终，chip 4906 只）

方法: `106d_v3_wired_full_chip` / forward_days=1 / threshold=3% / V3 侧车数据接入

| 窗口 | AUC | RankIC | Top5 正例率 | Top10 | 基准(随机) | 训练样本 | 测试样本 |
|------|-----|--------|-------------|-------|-----------|----------|----------|
| W1（2026-03） | **0.6996** | 0.2241 | 0.305 | 0.268 | 0.119 | 1,338,277 | 109,005 |
| W2（2026-06~07） | **0.6310** | 0.1618 | 0.275 | 0.258 | 0.149 | 1,647,279 | 217,960 |

- 106 维 = 82 base + 8 derived + 6 chip + 10 tech，v3_wired=True 接资金流(3)/两融(2)/事件(2)
- base=96 维（`v25_base_ensemble`）
- AUC 全程 > 0.63、RankIC > 0.16、Top5 相对基准 2~2.6 倍 → **具备样本外预测力**
- W1→W2 衰减正常（市场结构切换），非崩塌

---

## WorkBuddy 已落地的交付物（Cursor 可直接接手）

| 交付 | 路径 | 用途 |
|------|------|------|
| 106 维模型本地化 | `D:/alphapilot/models_106d/` | v25_opt/base_ensemble_{1,2,3}.ubj + v25_meta.json + extra_factors.parquet；xgboost 验证 opt=106 特征 |
| 106 维 walk-forward 评估脚本 | `bt_research/wb_106d_walkforward.py` | 移植 train_v25 完整 106 维管线（V3 侧车 + chip + lhb + tech + 1 日标签），**定为官方评估基线** |
| 模型/数据下载同步 | `bt_research/download_106d.py` / `bt_research/sync_side_data.py` | 从服务器(150.158.100.236)拉模型 + 侧车数据 |
| 通达信 5 分钟转换 | `bt_research/tdx_lc5_to_parquet.py` | `C:\Program Files\drmsoft7a580\vipdoc\{sh,sz}\fzline\*.lc5` → `D:/alphapilot/data/kline5m_tdx/`（到 08-06） |
| 最终结果 JSON | `output/wb_106d_walkforward_FINAL_20260807.json` | 106 维 W1/W2 全部指标 |
| 服务器数据更新 | kline_all.parquet(08-06 版) / chip_data_all.json(4906 只) | 旧 08-02 备份为 .bak_0802 |

> 旧 67 维 walk-forward 脚本**作废**，不要再作为评估口径。

---

## 给 Cursor 的行动项（按优先级）

### P0（本周）— ✅ 已全部落地
1. **评估基线切换**（✅ P0-1）：把 `wb_106d_walkforward.py`（106 维 / 1 日标签）定为官方 OOS 评估标准；服务器旧 67 维 walkforward 已下线，归档 `.disabled_20260807`。
2. **补数据缺口**（✅ P0-2）：
   - event 213/5000 → **4188 只 (84%)** ✅ cron 已加
   - lhb 龙虎榜 2201/5000 → **3067 只 (61%)** ✅
   - margin → **4093 只** ✅
3. **SSH 根因纠正**（✅ P0-3）：150.158.100.236 本身正常，之前"40+ 次失败"是**本地 `exec_command(timeout)` 对长任务误超时**（命令实际都成功）；已落地 `bt_research/srv_ssh.py` 稳健封装。

### P1（2-4 周）— 已就绪，按「先小批量验证再全量」推进
4. **资金流深度**：134 天 → 250+ 天（tdxhub 20 天 + 东财 120 天 → 拼新浪 250/500 天，两源相关 0.85–0.97）。**先 250 天跑 walk-forward 验证 AUC 提升，再扩 500 天**。
5. **标签对照**：在 106 维 1 日基线之上，先跑**评估型** 3 日标签对照（持有 1/2/3 天收益分布），**不急重训 3 日标签模型**。
6. **每日健康度口径修正**：把「模型状态感知/降权」逻辑改为「数据质量监控」（缺哪类数据报警），不轻易降权有效模型。

### P2（09 月）
7. 分市场建模（主板 / 创业 / 科创 20cm 结构不同）。
8. 月度滚动重训（walk-forward 自动化）。
9. 盘中信号接入（本地已能拿通达信 5 分钟数据到 08-06，可接 QMT）。

---

## 给钱多爸的拍板句（✅ 已核准 2026-08-07 15:24）

> **核准：生产 106 维模型 OOS 有效（W1≈0.70 / W2≈0.63）定论坐实，提纲「W2 失效 0.533」= 评估口径错配，撤回降权建议。P0 已全部落地：106 维基线切换、event 84% / lhb 61% / margin 4093 数据补齐、SSH 根因纠正（本地脚本超时非服务器故障）。下一步 P1：资金流 250+ 天拼接（先 250 天 walk-forward 验证）+ 3 日标签对照（先评估型）。**

---

## 状态

- [x] 106 维 walk-forward 完整重跑（chip 4906，W1/W2 数据落定）
- [x] 维度链铁证（67→93→101→106→116）
- [x] 模型 + 侧车数据 + 通达信 5 分钟本地化
- [x] 本告知文档落地 `docs/CURSOR_106D_OOS_VERDICT.md`
- [x] Cursor 复现官方基线（P0-1）→ W1 0.6995 / W2 0.6301，与 FINAL JSON 差 ≤0.001，见 `CURSOR_106D_OOS_VERDICT_RECEIPT.md`
- [x] Cursor 补 event / lhb / margin 数据（P0-2）→ event 4188(84%) / lhb 3067(61%) / margin 4093，cron 已加
- [x] SSH 根因纠正（P0-3）→ 本地 `exec_command(timeout)` 误超时，非服务器故障；落地 `bt_research/srv_ssh.py`
- [x] 钱多爸确认拍板句（2026-08-07 15:24，已更新数字版）
- [x] WorkBuddy 生成本地完整核查报告 `docs/WB_选股模型准确率核查_2026-08-07.md`（整合 106 维 OOS + Cursor 复现对齐 + P0 落地 + 拍板句）
- [x] （Cursor→WorkBuddy 回执）`docs/CHANGES_TO_WORKBUDDY.md` 六章已追加，WorkBuddy 已读
