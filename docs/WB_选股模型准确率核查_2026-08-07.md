# WorkBuddy 选股模型准确率核查报告（106 维 OOS 定论）

> 日期: 2026-08-07 | 作者: WorkBuddy（评估/诊断） | 状态: ✅ 已核准
> 关联: 服务器同步稿「选股模型准确率提升讨论」、Cursor 落地回执 `CURSOR_106D_OOS_VERDICT_RECEIPT.md`

---

## 一、问题起源

服务器同步稿给出三窗口 AUC：W0 0.5993 / W1 0.6394 / W2 0.5330，据此提出「状态感知 / 模型降权」候选假设，认为 W2 模型退化。

WorkBuddy 独立核查发现：**0.533 来自旧 67 维评估管线**（资金流仅 2 只 + 5 日标签），与生产 106 维模型（1 日标签）**口径错配**，不能据此判定模型退化。

---

## 二、维度链铁证（同一框架，只换维度，标签统一 1 日）

| 维度 | W2 AUC | 说明 |
|------|--------|------|
| 67 维（旧管线 / 提纲原跑） | **0.533** | 资金流仅 2 只，侧车几乎没接 |
| 93 维 | 0.533 | 仍缺 chip / 侧车 |
| 101 维（本地 chip 2183 只旧） | 0.631 | 补侧车后跳升 |
| **106 维（生产维度，chip 4906 只完整）** | **0.631** | ✅ 生产模型口径 |
| 116 维（fd1 实验维度） | 0.675 | 非生产维度，仅供参考 |

**结论**：0.533 → 0.631 的跃升 100% 来自「补维 + 接 V3 侧车数据」，与模型权重无关。提纲的 0.533 是拿旧 67 维管线（且用了 5 日标签，与生产 1 日标签也不一致）去测生产模型，属于口径错配。

---

## 三、生产 106 维完整 OOS 结果（最终，chip 4906 只）

方法: `106d_v3_wired_full_chip` / forward_days=1 / threshold=3% / V3 侧车数据接入

| 窗口 | AUC | RankIC | Top5 正例率 | Top10 | 基准(随机) | 训练样本 | 测试样本 |
|------|-----|--------|-------------|-------|-----------|----------|----------|
| W1（2026-03） | **0.6996** | 0.2241 | 0.305 | 0.268 | 0.119 | 1,338,277 | 109,005 |
| W2（2026-06~07） | **0.6310** | 0.1618 | 0.275 | 0.258 | 0.149 | 1,647,279 | 217,960 |

- 106 维 = 82 base + 8 derived + 6 chip + 10 tech，v3_wired=True 接资金流(3)/两融(2)/事件(2)
- base = 96 维（`v25_base_ensemble`）
- AUC 全程 > 0.63、RankIC > 0.16、Top5 相对基准 2~2.6 倍 → **具备样本外预测力**
- W1→W2 衰减正常（市场结构切换），非崩塌

---

## 四、Cursor 官方复现对齐（独立验证）

Cursor 独立复现官方基线：**W1 0.6995 / W2 0.6301**，与 WB FINAL JSON（0.6996 / 0.6310）差 ≤0.001 → 「W2 失效 0.533 = 口径错配」定论坐实。

---

## 五、P0 落地情况（已全部完成）

- **P0-1 基线切换**：106 维 walk-forward 定为官方评估标准；旧 67 维归档 `.disabled_20260807`
- **P0-2 数据补齐**：event 213 → **4188 只 (84%)**、lhb 2201 → **3067 只 (61%)**、margin → **4093 只**；cron 已加
- **P0-3 SSH 根因纠正**：150.158.100.236 本身正常，之前「40+ 次失败」是**本地 `exec_command(timeout)` 对长任务误超时**（命令实际都成功）；落地 `bt_research/srv_ssh.py` 稳健封装

> ⚠️ 认知纠正：之前排查方向曾误判为「服务器 SSH 不稳」，实为本地调用方式问题。服务器无需背锅。

---

## 六、优化建议（更新版）

### P0（已落地）— 评估基线 + 数据补齐 + SSH 封装
### P1（2-4 周）— 先小批量验证再全量
1. **资金流深度**：134 → 250+ 天（拼新浪 250/500 天，两源相关 0.85–0.97）。**先 250 天 walk-forward 验证 AUC 提升，再扩 500 天**。
2. **标签对照**：先跑**评估型** 3 日标签对照（持有 1/2/3 天收益分布），**不急重训 3 日标签模型**。
3. **健康度口径**：「模型状态感知/降权」→「数据质量监控」（缺哪类数据报警），不轻易降权有效模型。

### P2（09 月）
4. 分市场建模（主板 / 创业 / 科创 20cm 结构不同）
5. 月度滚动重训（walk-forward 自动化）
6. 盘中信号接入（本地通达信 5 分钟数据到 08-06，可接 QMT）

---

## 七、拍板句（✅ 已核准 2026-08-07 15:24）

> **核准：生产 106 维模型 OOS 有效（W1≈0.70 / W2≈0.63）定论坐实，提纲「W2 失效 0.533」= 评估口径错配，撤回降权建议。P0 已全部落地：106 维基线切换、event 84% / lhb 61% / margin 4093 数据补齐、SSH 根因纠正（本地脚本超时非服务器故障）。下一步 P1：资金流 250+ 天拼接（先 250 天 walk-forward 验证）+ 3 日标签对照（先评估型）。**

---

## 八、下一步行动（待 Cursor 执行）

- [x] **3 日标签评估型对照（已完成 2026-08-07）**：初版 2 窗口（66 交易日）结论「持有1天最优」被钱多爸质疑数据太少 → **扩展为 14 个月滚动窗口（270 交易日 / 133 万样本）重做，结论修正**：模型选股有显著超额（Top10 日度超额 +3.4%/天，t=17.6），且**持有 3 天（+5.29%）优于 1 天（+3.48%）**，剔除敏感月/分市场稳健（正式结果 `output/wb_hold_horizon_eval_v2_20260807.json`）
- [x] **资金流 500 天拼接 + walk-forward 验证（已完成 2026-08-08）**：新浪 500 天（2024-07 起）全量拉取 5006 只、重叠区比值中位 1.0（同源无缝拼接）、生产已切换（备份 `.bak_135d`）；14 窗口 AUC 提升 11/14 个月、Top10 日度超额 t 17.6→**20.2**（`output/wb_hold_horizon_eval_v2_20260807_ff500.json`）
- [x] **3 日标签重训对照（已完成 2026-08-08）→ 不重训**：AUC 1日胜 13/14 个月；Top10 持有3天 1日标签 +5.50% vs 3日标签 +3.66%（减半）。**保持 1 日标签训练 + 实际持有 3 天 = 最优策略**（`output/wb_hold_horizon_eval_v2_20260807_fd3.json`）
- [ ] 两项均按「先小批量验证再全量」推进 → **P1-4/P1-5 已按此完成，可关闭**

---

*本报告由 WorkBuddy 生成并落地本地 `docs/`；服务器版本（08-06 旧数据）已被本版取代。*
