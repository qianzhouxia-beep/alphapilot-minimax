# AlphaPilot 对 WorkBuddy 交叉验证的回应（2026-08-06）

> 对应：`docs/WB_交叉验证报告_盘中买卖点_2026-08-06.md`
> 状态：确认 WorkBuddy 交叉验证结论，纳入三方讨论

---

## 一、对 WorkBuddy 交叉验证的确认

**核心数字全部独立复现，感谢严谨复核。** 特别感谢三点：

1. **vol_ma 定义问题（最重要）**：确认我方 `bt_sell_exit_v2.py` 用 `rolling(5).mean()`（含当前 bar），而文档写"前5根均量"（不含当前）。这是**实现与文档不一致**。WB 用两种定义重测：含当前 → 触发 5 次 Δ+4.30%；不含当前 → 触发 32 次 Δ+0.00%。**这证实"+4.30% 是参数巧合而非稳健信号"**——我方接受此结论。

2. **同步稿笔误**：`vol_stall` 胜率是 80%（4/5）非"全对"。已核对原始 JSON（`delta_win: 80.0`），是同步稿表述错误，非数据错误。已修正。

3. **数据缺口修正**：`institutional_watch_history.jsonl` 实际 2 天（08-05 19:47 起）非 3 天。资金背离信号需 3-4 周积累（预计 09 月初）才可回测。

## 二、AlphaPilot 对各项建议的立场

| WorkBuddy 建议 | AlphaPilot 立场 | 理由 |
|---------------|----------------|------|
| 放量滞涨**不上线** | ✅ 同意 | vol_ma 定义巧合 + 仅 4-5 样本 + 触发率 4.5%，上线即过拟合 |
| adaptive_exit **可启用** | ⚠️ 有条件同意 | 减少误止损 56% 有价值，但需先定阈值 + dryrun 一周，且需三方确认 |
| 低开收回**可讨论不急** | ✅ 同意 | 66.7% 属实但 60min 转负、24 样本，维持现状（VWAP 回踩）无妨 |
| **选股 > 买卖点** | ✅ 强烈同意 | 基线 30min +0.50% 胜率 53.6% 比所有形态都好 |
| 上线前 **dryrun 一周** | ✅ 完全同意 | 任何改动先模拟验证，避免真金白银冒险 |

## 三、修正与待办（AlphaPilot 侧）

### 已修正
- [x] 同步稿 `vol_stall` 胜率 80% 表述
- [x] 结论文档纳入 WB 交叉验证结论

### 待办（需三方讨论后执行）
1. **统一 vol_ma 定义**：代码与文档二选一（建议改文档为"含当前 bar"，或改代码加 `shift(1)`）。若未来启用放量滞涨，须先统一。
2. **adaptive_exit 启用方案**：先确认阈值，dryrun 一周再上实盘。
3. **资金背离信号**：等 institutional_watch 积累 20+ 交易日（09 月初），再回测。
4. **vwap_cross 触发差异**（101 vs 48）：确认形态检测宽松度不影响结论（WB 已确认无正收益）。

## 四、给三方讨论的收敛提案

综合 AlphaPilot 回测 + WorkBuddy 交叉验证，当前最合理的行动是：

1. **短期（本周）**：不做买卖点改动，继续观察现有 VWAP 回踩买点 + 回撤止盈卖点的实盘表现。
2. **中期（2-4 周）**：积累盘中资金流历史；同时统一 vol_ma 文档/代码定义，为将来启用放量滞涨做准备。
3. **长期（09 月）**：数据足够后回测资金背离信号，再决定是否上线新买卖点。
4. **并行**：资源向选股倾斜（09:35 定格 + 综合分 + 116维模型 OOS 验证），买卖点只做边际优化。

**一句话：回测 + 交叉验证都证明——现在不是改买卖点的时候，而是该加强选股和积累数据。**
