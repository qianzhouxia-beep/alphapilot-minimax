# 给 Cursor 的一页纸简报：盘中买卖点研究收敛结论（2026-08-06）

> 用途：本简报为 AlphaPilot ↔ WorkBuddy 双方交叉验证后的**最终收敛结论**。
> 凡是涉及 `intraday_low.py` / `trade_executor.py` / `adaptive_exit.py` 的改动，请先读本简报，避免踩已排除的坑。

---

## 一、当前实盘代码状态（勿改动）

| 模块 | 现状 | 说明 |
|------|------|------|
| `intraday_low.py` | **VWAP 回踩 0.8% 低吸**（09:35~10:30，未触发 abandon） | 2026-08-06 曾短暂加入「低开收回」后**已回滚** |
| `trade_executor.py` | Plan C：硬止损-5%、开盘保护-7%、跌停-9.5%、趋势回撤6%止盈、超期15日 | 曾短暂加入「放量滞涨止盈」后**已回滚** |
| `adaptive_exit.py` | 已写好但**未启用**（`c_atr_adaptive: False`） | 保持现状 |

**三个文件 MD5 与改动前完全一致，无未提交残留。**

---

## 二、已排除的信号（不要重新建议）

### ❌ 放量滞涨止盈 —— 确认过拟合，不上线

- 表面回测 Δ+4.30%（触发 5 次，胜率 80%），但：
  - **vol_ma 定义巧合**：均量含当前 bar → +4.30%；不含当前（文档字面"前5根均量"）→ **Δ+0.00%**（32 样本）
  - 仅 4-5 个样本、触发率 4.5%，任何参数微调都是过拟合
- **结论：即使看到诱人的回测数字，也不要单独据此建议上线。**

### ⚠️ 低开收回买点 —— 属实但暂缓

- 30min 胜率 66.7% 属实（24 样本），但 60min 转负（-0.96%），像短脉冲
- **维持现状（VWAP 回踩）无妨，可作为备选等待更多样本**

### ⚠️ adaptive_exit —— 未启用，若启用需三方确认

- 价值是**风控**（止损 25→11 次，-56%），**不是收益**（Δ+0.03%≈0）
- 启用条件：先定阈值 + dryrun 一周 + 三方确认

---

## 三、数据现状与积累目标

| 数据 | 现状 | 何时够用 |
|------|------|---------|
| `output/institutional_watch_history.jsonl` | 仅 **2 天**（08-05~06） | 20+ 交易日 ≈ **09 月初** |
| `data/fund_flow_history.json` | 5003 只 × 134 天 | 可做日度近似回测 |
| `data/kline5m/` | 07-23 起 11 个交易日 | 仅够短线形态 |

**资金背离买卖点信号：现在不可回测，等 09 月初。**

---

## 四、待办（三方共同，Cursor 侧可执行项）

1. **统一 vol_ma 定义**：`bt_research/bt_sell_exit_v2.py` 用 `rolling(5).mean()`（含当前 bar），文档写"前5根均量"——**代码与文档必须二选一**（建议文档改为"含当前 bar"，或代码加 `shift(1)`）。这是将来启用任何量能信号的前置。
2. **积累数据**：`institutional_watch` 保持 systemd 运行，别停。
3. **选股优先**：资源向 09:35 定格 + 综合分 + 116 维模型 OOS 验证倾斜，买卖点只是边际优化。
4. **任何改动上线前**：dryrun 至少一个交易周。

---

## 五、一句话总结

> **回测 + 交叉验证都证明：现在不是改买卖点的时候，而是加强选股 + 积累数据。**
> 表面上最诱人的信号（放量滞涨 +4.30%）恰恰是过拟合——这就是独立验证的价值。

---

## 附：参考文档（服务器 `/home/ubuntu/alphapilot/docs/`）

- `WB_交叉验证报告_盘中买卖点_2026-08-06.md`（WorkBuddy 独立验证）
- `AlphaPilot对WB交叉验证的回应_2026-08-06.md`
- `盘中买卖点回测结论_2026-08-06.md`
- `给WorkBuddy的同步稿_盘中买卖点_2026-08-06.md`
- 回测脚本：`bt_research/`（`bt_buy_entry.py`, `bt_sell_exit.py`, `bt_sell_exit_v2.py`, `bt_vol_adaptive.py`, `wb_cross_verify.py`, `wb_vol_stall_grid.py`）
