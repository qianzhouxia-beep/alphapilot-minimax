# 116 维模型部署交接单 — 供 WorkBuddy 复核

**日期**: 2026-08-04 17:20
**作者**: AlphaPilot 生产维护（Human + Agent 协同）
**状态**: 部署完成，全链条试运行中

---

## 一、回测结果（部署依据）

同一回测窗口 `2026-06-08 ~ 2026-08-03`（40 交易日，top_n=2，T+1 开盘买 / T+2 收盘卖 / 成本 15bp / ≥3% 命中）：

| 指标 (A1_permission) | 93 维生产（旧） | **116 维候选（新）** |
|---|---|---|
| win_rate | 45.0% | **65.0%** |
| hit_3pct_rate | 32.5% | **47.5%** |
| avg_return | 1.40% | **4.07%** |
| total_return | 28.1% | **114.1%** |
| max_drawdown | -17.7% | **-9.1%** |

- 116 维候选在**所有指标上显著优于** 93 维生产（胜率 +20pp、命中率 +15pp、回撤减半）。
- 回测产物：`rd_workshop/candidates/track_a_current_model_20260803_232009/v3_tradable_gated_sleeve_backtest_NEW.json`

## 二、评分区分度验证（关键：此前 v25-93 维被回滚的原因已解决）

116 维模型全市场评分区分度实测（200 只样本，42ms/只）：

- min=0.0755, max=0.6937, **std=0.1031, 带宽=0.618**
- p10=0.209 / p25=0.290 / p50=0.370 / p75=0.433 / p90=0.484

> 对比：旧 v25-93 维全市场评分几乎不区分（0.5592~0.5605，带宽 0.0013）。**116 维含 RD 因子 + 全量筹码后区分度恢复。**

## 三、部署清单（已完成）

1. **模型文件** → 生产 `models/`（候选 `track_a_current_model_20260803_232009/models`）：
   - `v25_opt_ensemble_{1,2,3}.ubj`（116 维）
   - `v25_base_ensemble_{1,2,3}.ubj`（96 维，配套）
   - `extra_factors.parquet`（10 个 RD 因子，71943 行）
2. **meta** → `models/v25_meta.json` 更新为 116 维配置（`extra_factor_columns` 10 项、`extra_factors_path` 指向生产）
3. **代码切换** → 显式 `load_model("v25")`：
   - `recommend.py`（选股主链）
   - `api_server.py`（前端评分）
   - `api_server_v2.py`
   - （`analysis_engine.py` 共享模块级 screener，无需改）
4. **服务** → `alphapilot-api.service` 已重启，健康检查 200
5. **验证**：生产 `vm25_scorer` 加载 = 116 特征 / 3 模型 / 10 RD 因子 / 芯片 4992 只 ✅

## 四、备份（可回滚）

- `models/backup_v25_20260804_pre116/` — 部署前的 93 维全套（含 16:33 被外部替换的 base_1/2）
- `models/backup_v25_20260804_1635/` — 16:52 外部进程创建的备份（与上面内容不同）
- `recommend.py.bak_20260804_1714` — recommend 切换前版本
- `output/daily_recommend.json.bak_20260804_1719` — 试运行前的每日推荐

## 五、⚠️ 需要 WorkBuddy 确认的事项

1. **16:33/16:34 外部进程替换了生产 `models/v25_base_ensemble_1/2.ubj`**（96 维，特征与候选一致但文件不同，md5 不同）——不是本次部署所为。**16:52 创建 `backup_v25_20260804_1635`**。请确认：是不是 WorkBuddy 或另一会话在准备 v25 相关操作？如果是，请说明意图，避免并发写冲突。
2. 本次部署已用候选 base_1/2/3（96 维）**覆盖**了上述外部写入的 base_1/2，保持 base+opt 配套一致。

## 六、全链条试运行结果（已完成 17:21）

`logs/trial_run_v25_20260804.log`：

- **模型加载** ✅ `VM2.5 loaded: v25_opt x3 feats=116` + `extra factors loaded: 10 cols`
- **事件数据** ✅ 龙虎榜 637 行（业绩预告 0 行——数据源固有局限）
- **隔夜情绪因子** ✅ 美股情绪分 0.538 → 88 个受益板块（算力/半导体/通信/电子/芯片 +12%），Top5 中 2 只带"隔夜 +0.8%"标记
- **全市场评分** ✅ 4997 只扫描 / 4982 只有效 / **耗时 2177s（36分钟）**
- **评分区分度良好** ✅ Top20=0.8130→0.6035 梯度明显，中位 0.5050，min=0.4799（对比 93维全挤 0.559，带宽 0.0013）
- **Top5 样例**：传智教育 0.8130 / 欣天科技 0.8005（隔夜）/ 利柏特 0.7736 / 百花医药 0.7459 / 恒银科技 0.7143（隔夜）
- 结果已落 `output/daily_recommend.json`（500 只，model=vm25, n_features=116）

## 六.5、试运行中发现并修复：筹码过滤失效（P0）

**现象**：试运行日志出现 `[筹码集中度过滤] 跳过(异常): '>=' not supported between instances of 'str' and 'int'`。

**根因**：`chip_data_all.json` 两处（根目录 + `data/`）中 **1503 只股票（子代理补拉）的 5 个数值字段是字符串**（如 `chipConcentration70='19.89'`），其余 3489 只是 float。混合类型导致 `features.compute_chip_features` 与 `recommend.py` 的筹码集中度过滤在数值比较时抛 TypeError → 过滤整段被跳过（本应剔除的筹码峰分散股漏入）。

**修复**：
1. **数据修复**（一次修复全读方受益）：`_fix_chip_types.py` 将两个文件的 7515 个字符串数值字段转 float，备份 `.bak_typefix_20260804_182443`。验证：剩余字符串=0，筹码过滤恢复正常（剔除 70 只，异常 0）。
2. **代码防御**（防未来复现）：`features.py` 的 `compute_chip_features` 加 `_num()` float 防御转换，备份 `features.py.bak_typefix_20260804`。语法 OK，字符串模拟测试不崩。

> 注意：`data/fund_flow_cache.json` 当前仅 1 只（15:27 被外部进程覆盖）。`features.py` 以 `fund_flow_history.json`（5000 只，08-04 15:30 更新）优先，不影响模型特征；但若 WorkBuddy 会用 `fund_flow_cache`，建议留意。

## 七、明天（08-05）链路预期

```
05:00  pipeline → recommend.py（v25 116维，实测 2177s=36 分钟 < 超时 3600s ✅）
09:35  live_momentum_scanner 读 daily_recommend → 重排写回（带 main_net）→ build_score_top10 生成三路融合 Top10
09:40  audit_score_top10_day --phase open
09:50  scores 同步 → QMT
16:15  fix_kline → 16:22 缓存重建
```

**结论**：回测、区分度、全链条试运行全部通过，已部署。试运行中发现并修复筹码字符串类型问题。请 WorkBuddy 复核回测口径、16:33 写入归属，并确认 `fund_flow_cache.json` 仅 1 只是否为预期。

## 八、K线并发限流问题修复（18:47，P0）

**现象**：今天 16:15/16:16 cron 的 `fix_kline_server.py`（WORKERS=16）拉取失败率 61-64%，08-04 K线仅 3060/4991 只（缺 1931 只深市）。

**根因**：mootdx 通达信直连对并发连接限流——实测 1 线程 20/20 成功、8 线程仅 10/20。

**修复**：
1. `fix_kline_server.py` WORKERS 16 → **1**（备份 `.bak_20260804_1845`）
2. 串行重跑：**4991/4991 成功，失败 0**，80 秒，08-04 覆盖 **100%**
3. 重建 `backtest_cache`（12s，最新日期 08-04）
4. 明日起 cron 16:15 用串行模式，不会再因并发限流失败

> 若 WorkBuddy 有类似批量拉取（mootdx），建议同样降并发。

## 九、生产操作锁（prod_op_lock，19:46 上线）——请 WorkBuddy 共同遵守

针对"多会话并行动生产互相覆盖"的确认，已上线锁文件机制：

**文件**：`/home/ubuntu/alphapilot/prod_op_lock.py`
**约定**：任何会**写生产数据**的脚本/操作，执行前先 `acquire_prod_lock(op, reason)`，结束后 `release_prod_lock()`；被占用则跳过并提示。

**已接入**：
- `fix_kline_server.py`（16:15 cron 写 K 线）
- `rebuild_backtest_cache.py`（16:22 cron 写缓存）
- `recommend.py`（05:00 评分，防中途被覆盖）

**规则**：锁超时 2h 自动视为陈旧可覆盖（防死锁卡死明天 pipeline）；同 pid 可重入；释放校验 pid。
**命令行**：`python3 prod_op_lock.py --check` 查看占用；`python3 prod_op_lock.py <op> --reason ...` 手动占用/释放。
**请 WorkBuddy**：若用 python 脚本操作生产数据，请同样 import prod_op_lock 包裹；若用 shell，可先 `python3 prod_op_lock.py --check` 确认空闲再操作。

> 锁不拦截只读操作（评分、读取），仅拦截写生产数据，避免误伤正常链路。

## 十、评分质量门控（score_quality_gate，20:00 上线）——宁可不操作

**背景**：用户明确决策——**不用 v18 回退（v18 出票质量差），v25 评分异常时宁可不选股/不交易**。

**机制**（双保险）：
1. **pipeline 05:00**：recommend.py 跑完后执行 `score_quality_gate.py --json --mark`
   - 通过（区分度正常）→ 继续后续门控
   - 异常 → 中止当天选股，给 `daily_recommend.json` 回写 `_quality: {ok:false, action:"NO_TRADE_TODAY"}`
2. **scanner 09:35**：读取 daily_recommend 时若发现 `_quality.ok == false` → **当天不选股直接返回**（不回退全市场扫描，宁可空仓）

**判定阈值**（依据 08-04 实测：正常 v25 带宽 0.276 / std 0.028；病态 93维 带宽 0.0013）：
- 有效评分 < 30 → 异常
- 带宽(span) < 0.05 → 病态区分度
- std < 0.01 → 分数无区分
- Top20 梯度 < 0.02 → 头部无梯度
- 中位数超出 [0.05, 0.95] → 分数越界

**已验证**：正常样本通过 / 病态样本拦截+打标 / scanner 读到标记正确停摆。
**备份**：`alphapilot_pipeline_v3.py.bak_qualitygate_20260804`、`live_momentum_scanner.py.bak_qualitygate_20260804`
**命令**：`python3 score_quality_gate.py --check` 可手动查看当前评分质量。

> 原则：**宁可空仓，不用劣质信号交易**。此机制保证任何一方（v25 模型异常 / 数据缺失 / 评分退化）都不会让劣质结果进入实盘。
