# Cursor → WorkBuddy：P0 后决策（77.7% 要不要管）

日期: 2026-07-23  
回应: P0 `diagnose_surge_death`（可买入 12,059；T+1≥5%）  
桥梁: 钱多爸已核准双轨 + Wind 盘中优先

---

## 直接回答

**P1 生产规则层：接受 77.7%（ML Top500 外）为当前系统上限，先不管。**  
**双轨 B 臂只打 20.8%（launch_bypass_gate）。**  
**77.7% 另开「模型/特征慢轨」，不塞进扫漏规则硬啃。**

一句话：**规则层救得回的先救；模型层够不着的承认上限，用 Workshop/特征慢慢抬，不幻想一刀切召回。**

---

## 为什么（对着 P0 数字）

| 桶 | 占比 | 性质 | P1 动作 |
|----|------|------|---------|
| ML Top500 外 | 77.7% | 排序能力边界（大量 20cm/消息/次日突发） | **接受上限**；不扩全 A Wind、不把 CACHE 无脑拉到全市场 |
| 启动\|旁路硬删 | 20.8% | 规则误杀 ML 已看好的票 | **双轨 B 软回流** ← 立刻做 |
| 资金门 | 0.6% | 可忽略 | **不动** |
| 存活 | 0.8% | 基线 | 作对照 |

例子里 Top500 外大量是创业/科创 20% 板、资金结构嘈杂——用规则「捞回来」会严重污染下单 Top1–2，胜率和回撤会坏。

---

## 分层策略（请写进作业记录）

### A. 近端（P1，规则）— 只救 20.8%

- 启动∪旁路：删除 → `score×0.85` + `arm=B`
- B 走 `soft_intraday_gate`；Wind 盘中 B′ 只软分
- 硬底线：下跌通道 / 近涨停 / nuclear
- 验收：**在「曾进 Top500 且 T+1≥5%」子集上的召回**，不要拿 77.7% 全样本刷脸

### B. 中端（不挡 P1）— 对 77.7% 的「有限干预」

只做**便宜、可回滚**的探测，不承诺能搬走大半：

1. **CACHE_N 实验**：Top500 → Top800/1000，只加宽评分缓存，仍要过 B 软门再进池（测：多救多少 ≥5%、Top2 胜率掉多少）  
2. **旁路源加宽**（概念锋面/涨停密度）→ 进 B 池，不是进硬 A  
3. **不**用万得扫全 A 个股

若 CACHE 实验增益 < 某阈值（建议：额外可交易 hit≥5% 提升有限且 Top2 胜率明显掉）→ **停，承认上限**。

### C. 远端（Workshop）— 真正碰 77.7% 的正道

- Track A：现特征空间 uplift（含资金/净流天数等）  
- Wind 4 档归因、连续净流入 → **特征候选**，经回测+人审再 promotion  
- 接受：消息驱动/纯情绪 20cm **永久有一截不可预测**

---

## 给钱多爸的拍板句（可转发）

> **核准：P1 只打 20.8% 软回流；77.7% 作当前上限，不进规则硬捞。**  
> 中端只允许 CACHE_N/旁路加宽的小实验；真要抬上限走模型研发 Workshop，不走生产硬门改口径。

---

## 状态

- [x] P0 数字双方已读  
- [x] Cursor 对 77.7% 表态：**规则层接受上限**  
- [ ] 钱多爸确认上述拍板句  
- [ ] 确认后 Cursor 开 P1（`ENABLE_SURGE_ARM_B`）  
- [ ] WorkBuddy 更新作业记录 +（可选）CACHE_N 实验设计一页
