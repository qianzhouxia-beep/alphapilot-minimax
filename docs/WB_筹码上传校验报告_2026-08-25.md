# 筹码上传校验报告 · 2026-08-25（铁律首执行日）

> 背景：2026-08-25 起强制「上传前必跑 `check_chip_batches.py`，三项全过才可上传」，源自 08-24 半截上传事故（1192 只停旧日期）。本文件为当日首次执行的复核记录。

## 一、批次完整性校验（本地）

运行：`python3 production_strategies/server/check_chip_batches.py --date 2026-08-25 --dir <WorkBuddy 批次目录>`

| 检查项 | 结果 | 阈值 |
|---|---|---|
| ① 批次号连续 | ✅ 101 批，序号 00–100 无缺口 | 00~N 连续 |
| ② 覆盖股票数 | ✅ 4992 只 | ≥ 4850 |
| ③ 最新日期 == 当日 | ✅ 全部 `2026-08-25` | == 2026-08-25 |
| **结论** | **EXIT_CODE=0，通过可上传** | — |

## 二、服务器落地核验（SSH 上海 150.158.100.236）

上传脚本 `_upload_chip_0825.py` 合并 4992 只后，经 paramiko 备份+双路径写入，验证两个目标文件：

| 路径 | 总数 | date 分布 |
|---|---|---|
| `/home/ubuntu/alphapilot/data/chip_data_all.json` | 4992 | `{2026-08-25: 4992}` |
| `/home/ubuntu/alphapilot/chip_data_all.json` | 4992 | `{2026-08-25: 4992}` |

→ 服务器筹码数据**全部刷新为 2026-08-25**，无旧日期残留。

## 三、结论

- ✅ **08-24 事故未重演**：今日为全量 4992 只、日期一致，非半截。
- ✅ 自动化（15:30 refresh / 15:50 MCP）当日按时产出批次并上传，落在 17:30 前，18:15 服务器复查可兜底。
- ⚠️ **一处待整改**：生成的 `_upload_chip_0825.py` **未内嵌** `check_chip_batches.py` 校验调用（铁律第 3 条要求上传脚本开头带校验，失败即 `exit(1)`）。本次因数据本身完整侥幸通过，但下次上传脚本应补上内嵌校验以实现双保险。

## 四、待办 → 已闭环（2026-08-25 23:25）

- [x] 上传脚本模板 — WorkBuddy 承诺 **08-26 起** `_upload_chip_template.py` 内嵌 check，FAIL 即 `exit(1)`
- [x] 服务器 WeCom FAIL-only — Cursor 已部署 `data_readiness_gate.py` → `maybe_wecom_push_fail()`
- [x] 分工对齐 — 见 `MEMORY.md`「日常验证分工约定」、`docs/LOG_2026-08-25.md`

## 五、闭环确认

**08-24 半截事故整改彻底封口。** 从 08-26 起：PASS 静默，FAIL 才报。
