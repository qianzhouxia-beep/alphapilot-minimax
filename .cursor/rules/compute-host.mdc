# 算力分工铁律 — 上海=生产机 / 新加坡=重算力沙箱（唯一权威）

> 2026-09-13 用户拍板。背景：在上海生产机上跑全市场回测，**两次被 OOM killer 杀掉**（上海只有 3.6 GB 内存）。

## 硬性规则

1. **上海服务器（`150.158.100.236`）= 生产机，只跑生产任务**：05:00 管线、09:35 scanner、09:36 导出、各类 cron、数据闸门。
   **禁止**在其上跑回测 / walk-forward / 训练 / 因子挖掘 / 大批处理 —— 内存 3.6 GB，会 OOM 并**可能连累生产链路**。
2. **重算力任务一律在新加坡服务器（`43.156.119.47`）沙箱跑**。该机 15.6 GB 内存 / 4 核 / 178 G 磁盘，且与上海生产隔离。
3. 沙箱根目录 **`/home/ubuntu/bt_sandbox/`**：
   - `alphapilot/` — 代码 + 数据镜像（`ALPHAPILOT_ROOT` 指向这里）
   - `pylibs/` — 与生产**同版本**的依赖（`PYTHONPATH` 指向这里）
4. **必须用同版本依赖**，否则结果不可比。运行前缀固定：

   ```bash
   ALPHAPILOT_ROOT=/home/ubuntu/bt_sandbox/alphapilot \
   PYTHONPATH=/home/ubuntu/bt_sandbox/pylibs \
   nice -n 10 python3 -u <脚本>
   ```

   | 依赖 | 生产（上海） | 沙箱（SG，已锁） | SG 系统默认（❌ 不可用） |
   |---|---|---|---|
   | Python | 3.10.12 | 3.12 | 3.12 |
   | numpy | 2.2.6 | 2.2.6 | 2.5.1 |
   | pandas | 2.3.3 | 2.3.3 | **3.0.3** |
   | xgboost | 3.2.0 | 3.2.0 | 3.3.0 |
   | sklearn | 1.7.2 | 1.7.2 | 1.9.0 |
   | scipy | 1.15.3 | 1.15.3 | 1.18.0 |
   | pyarrow | 24.0.0 | 24.0.0 | 25.0.0 |

5. **数据只从上海单向下拉**（SG→上海 免密：`-i /home/ubuntu/.ssh/alphapilot.pem ubuntu@150.158.100.236`）。
   **绝不反向写上海生产数据**；沙箱产物只留 SG。
6. 沙箱跑重活务必加 `nice -n 10` —— SG 同时跑着 crypto 纸盘 / HK 纸盘，别抢爆。
7. **口径与结论仍以上海生产为准**；SG 只是算力，不是数据源。用同版本依赖 + 同一份数据，目的是**逐位复现**上海结果（2026-09-13 已验证：300 只 smoke 两臂 AUC 与上海 16 位小数完全一致）。
8. 代码改动仍走 **repo-first**：先改本地仓库 → git push → 再同步到 SG 沙箱（见 `MEMORY.md` 代码流向铁律）。

## 什么时候可以留在上海跑

**只读、轻量、不 hold 大内存**（< 30 秒、< 200 MB）的任务可以留上海。判据：

- 要建千万行级面板 / 训练模型 / 多折回测 ⇒ **一律去 SG**。
- 单表读个几千行、查个状态、跑个闸门 ⇒ 留上海。

## 一键入口

`scripts/sg_sandbox.sh`（同步代码到沙箱 + 用锁定依赖跑指定脚本）。

## 变更记录

- 2026-09-13 建立（用户拍板）。触发：`rd_workshop/walkforward_oos.py` 全量跑在上海连续两次 OOM。
