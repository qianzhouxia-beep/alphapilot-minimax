# AlphaPilot 页面视觉统一 — 执行报告

## 任务概述
对 3 个 AlphaPilot 页面的 className 颜色变量和样式类进行统一切换，使用 CSS 变量代替内联颜色 hex 值，补充 `card-lift`、`data-table`、`info-card`、`tag-badge` 等工具类。

## 修改的文件

### 1. page.tsx（Landing 首页）
**替换总计：90 处**
- 标准颜色变量替换：bg-background、text-text-primary、text-text-secondary、text-text-disabled、text-status-success/warning/danger/info、border-border-subtle、bg-surface-container-low、bg-surface-panel
- `text-[#a2c9ff]` → `text-text-primary`（含带透明度后缀的变体）
- `glass-panel` 类名 → `glass`（className 中全部替换，CSS 定义保留）
- 4 张 feature 卡片添加 `card-lift`
- `text-[#c0c7d4]` → `text-text-secondary`
- `bg-[#4da3ff]` → `bg-status-info`
- 底部导航 link `hover:text-[#EAF2FF]` → `hover:text-text-primary`

### 2. cn/stock/page.tsx（股票详情）
**替换总计：92 处**
- 标准颜色变量全部替换
- 板块标签 → `tag-badge text-status-info`
- 区域标签 → `tag-badge text-status-success`
- 顶部 header 卡片 → `info-card card-lift`
- AI 评分决策卡 → `info-card card-lift`
- `rounded-lg bg-[#4DA3FF] px-4 py-2` → `rounded-lg bg-status-info`

### 3. cn/backtest/page.tsx（回测）
**替换总计：85 处**
- 标准颜色变量全部替换
- `<table>` 添加 `data-table` 类
- 配置面板添加 `card-lift`
- KPI 图/表格区域添加 `card-lift`

## 未修改的内容（保留原样）
以下为非主题色，不属于标准 CSS 变量映射，按任务要求保留：
- `#003866` — 按钮上的对比文字色（bg-status-info 按钮的文字）
- `#A78BFA` / `#F59E0B` / `#3B82F6` — 特殊 accent 色（阶段识别卡片用）
- `#ff7a7a` — hover 态的删除按钮色
- `#35e0a3` — 渐变色中的绿色段（from-status-info to-#35e0a3）
- `.glass-panel` CSS 定义（`<style jsx>` 中，不是 className）
- 所有功能逻辑、API 调用、数据字段、函数签名未动
