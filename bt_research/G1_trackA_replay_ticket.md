# Ticket：G1 × 完整 Track A 卖出规则 replay（定论"G1 到底能不能赚"）

- **状态**：📋 待启动（老板 2026-09-11 同意列为下一步）
- **目标**：用**交易端** Track A 的**完整**卖出规则（非 peel 核心近似），在真实 5m/日线数据上回放 G1 分组（走高/走低），给出"G1 作做多前提 / 作否决器"的**生产口径**定论。
- **为什么**：§九 的 peel 近似给出"走高侧显著（t 3.2~5.1）"，但**未含** Track A 的 VWAP 破位、Wyckoff BC、T+2 条件强平、弱市 floor、TSDOWN、D8 观察仓等。缺这些就仍是近似，不足以对外定论。

---

## 1. 回放对象与口径

- **入场**：D 日 `bar(09:35).close`（= 09:36 生产买入价）。
- **样本**：`wb_top10_gap_prod_results.json` 的 263 笔（主窗口 + 扩样），按 `cat` 分走高/走低。
- **出场**：Track A **sim v2.45** 全套规则（见 §2）；另跑 **live v2.38** 对照（首触 peel、`pb=min(0.05,…)`）。
- **评价**：逐笔 t + **按天聚类 t** + **同日配对 t**（三件套，共采纪律）。

## 2. Track A sim v2.45 卖出分支（须逐条实现）

按代码判定顺序（`_check_sell`）：

| # | 规则 | 触发 | 动作 | 离线可复现? |
|---|---|---|---|---|
| 0 | 异常防呆 | `daily <= ANOMALY_PCT` | 持有 | ✅ |
| 1 | 跌停 | `daily <= -9.7%` | 全卖 | ✅ |
| 2 | T+1 | 当日买入 | 不可卖（除跌停） | ✅ |
| 3 | D3-1 固定止损 | `ret <= -4%` | 全卖 | ✅（D8 豁免需观察名单） |
| 4 | TSDOWN | TrendState 新转 DOWN（窗口）| 卖半 | ⚠️ 依赖 TrendState/评分 |
| 5 | Wyckoff BC | `wy_bc_armed` + 窗口 | 全卖 | ✅（需 5m+日线量） |
| 6 | VWAP weak-early | `vwap_broken` + 次晨确认 | 全卖 | ✅（需 day-VWAP） |
| 7 | 硬止损 | `ret <= hs`（默认 −10%，vol 自适应）@≥14:45 | 全卖 | ✅ |
| 8 | T+2 条件强平 | @14:45：动态 floor / 延期 T+3 | 全卖/延期 | ✅（floor 依赖振幅+年化波动） |
| 9 | **自适应 peel** | `arm ret>=ta(3%)` → 峰值回撤 `pb` → 卖半，≤2 刀 | 半/清 | ✅ |
| 10 | 弱市收紧 | `weak` → floor 收紧 / MA25 判定 | 调参 | ✅（需弱市判定口径） |

**依赖函数**：`_adaptive_params`（年化波动）、`_weak_regime`、`_day_vwap`、`_ma25`、`_wyckoff_holding_bc`、`_t2_force_floor`、`_hold_days`（**交易日**计数）。

## 3. 数据依赖与可行性（步骤 0 实测完成 2026-09-11）

| 数据 | 用途 | 状态 |
|---|---|---|
| `data/kline5m/<code>.parquet` | 日内逐 bar 卖点 | ✅ **263/267 全覆盖、0 缺文件**；历史跨 **2026-03-02 → 09-11（~6 个月）** |
| `data/kline_cache/kline_all.parquet` | 日线：prev_close / MA25 / 年化波动 / T+2 | ✅ 有 |
| TrendState 状态 | TSDOWN 半卖 | ⚠️ 由服务器 issue#6 链路产出，**离线可能不可得** |
| D8 观察名单 | −4% 固定止损豁免 | ⚠️ 同上，可能需从 `output/` 取历史 |

**步骤 0 结论**：可行。唯一缺口 = 4 笔（`001309`/`300475` 07-27、`688523`/`600118` 07-30 无 `09:35` bar），与既有 263 样本一致，不额外损失。

## 4. 实施步骤

0. **数据可得性检查**：确认窗口内每笔的 5m 覆盖 + 日线覆盖；列出缺失票。
1. 抽出 Track A 的卖出规则为**纯函数式 replay**（不依赖 QMT 运行时），逐条 assert 对齐源码常量。
2. **自检基线**：`hold` 口径必须复现 WB 的 **+0.55% / 49.4%**；`peel-only` 必须复现 §九 的 live/sim 数（防止重蹈"数值不对"）。
3. 跑闭环：全套规则 × 走高/走低 × live v2.38 / sim v2.45。
4. 对不可复现项（TSDOWN / D8）做**敏感性**：含 / 不含各跑一次，报告差异。
5. 出结论：G1 作**做多前提**是否成立、作**否决器**是否成立；三件套 t 全报。

## 5. 验收 / 反例检查

- [ ] `hold` 基线复现 +0.55% / 49.4%（硬门）
- [ ] peel-only 复现 §九 表（硬门）
- [ ] 每笔卖出都能在源码找到对应分支（可追溯）
- [ ] 三件套 t 全报，不只逐笔
- [ ] 明确标注哪些规则未纳入（若 TSDOWN/D8 不可得）

## 6. 风险

- ~~**5m 历史仅 ~34 天**~~ → **已实测排除**：样本窗口 263/267 全覆盖，历史跨 ~6 个月（03-02→09-11）。
- **TrendState / D8 离线不可得**：TSDOWN 半卖与 −4% 豁免可能只能近似或排除 → 在结论里显式降级（步骤 4 做敏感性）。
- **入场价**：本回放只用 5m 09:35；与真实 QMT 成交可能有滑点（QMT 端另有 P2 触发逻辑，本回放不重建 P2）。

## 7. 关联

- 近似版结果与更正：`knowledge/inbox/2026-09-11-gap-bug-recheck.md` §八/§九
- 口径定调：`knowledge/decisions/2026-09-11-code-flow-and-two-ends.md`
- 源码：`production_strategies/track_a/TrackA_track_a_qmt_full_chain_sim.py`（v2.45）、`..._live.py`（v2.38-tpl）
