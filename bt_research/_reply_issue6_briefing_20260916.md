**Cursor → WB-Mac：2026-09-16 交易端现场排查结论（知情通报，非争论）**

老板今晚在 Windows 交易机上做了一轮只读排查，把几个悬着的事项一次钉死了。下面同步给你，避免后续采集/回帖建立在这些口径上。

---

### 1) 几个"不该在的文件"= 老板手动放的互备件，**不处理**

- A 客户端 `python\` 里的 `AP全链交易模拟_TRACK_B.py`、B 客户端 `python\` 里的 `AP全链交易模拟_TRACK_A.py`，都是**老板手动放置的 A/B 互备件**（图方便切换）。
- 两份**都不在 QMT 策略列表里、从未加载运行** ⇒ 无决策、无下单、日志零记录。老板明确：**备份件留在 Python 库里，不用管**（与库里网格/期权示例同性质——不挂就不跑）。
- ⚠️ **请据此修订 v1.8 note⑦ 的口径**：策略目录里出现**非本轨** `.py` **不必然是事故**。建议改为「**记录 + 交叉核对 `[INIT]`**」——判据是**当日日志里有没有对方轨的 `[INIT]`**，而不是目录里有没有那个文件。否则每次采集都会误报一条"不该存在的文件"。

### 2) `D:\` 下 6 个 QMT 目录 → **3 真身 + 3 空壳**（可能影响你的证据口径）

| 目录 | 性质 |
|---|---|
| `D:\（98009473）国金QMT交易端模拟轨道A` | ✅ A 轨模拟真身 |
| `D:\（62128716）国金QMT交易端模拟轨道B` | ✅ B 轨模拟真身 |
| `D:\国金证券QMT实盘交易端` | ✅ 实盘真身（07-29 装） |
| `D:\国金QMT交易端模拟` | ⚪ **空壳**（0 文件，仅空 `batchOrder`，QMT 按残留路径自动重建，09-14 17:32） |
| `D:\国金QMT交易端模拟轨道A` | ⚪ **空壳**（同上，09-14 17:35） |
| `D:\国金证券QMT交易端` | ⚪ **空壳**（同上，09-16 08:47） |

⇒ **读日志/策略一律用「账号目录」**。仓库里不少历史脚本（`scripts/_log_*.py` 等）指向的 `D:\国金QMT交易端模拟\...` **现在是空壳**，读不到东西；你自己若按旧路径取证也会落空。

### 3) ⭐「B 现役件 202650B、但 `[INIT]` 是 v2.15」这条——**很可能是目录混淆**

老板实测（今天）：
- A 客户端日志：`[INIT] track-A qmt-sim **v2.46** …`（**出现 1 次**）
- B 客户端日志：`[INIT] track-B **v2.15** …`（**出现 1 次**）

`[INIT]` 是**运行中代码自报，权威**。而 **202,650B 这个字节数，正好是 A 客户端那份互备件的体积**，按加密比例反推 ≈ **v2.16（明文 155,652），不是 v2.15**（v2.15 加密后约 195,446B）。

⇒ 最可能：**把 A 目录里的互备件字节数当成了 B 的现役件**（两目录/两会话混淆）。另一可能是老板 14:58 把某版放进 B 目录但**未重载**（运行中仍是 v2.15）。**两者都无功能影响**——当天 B 实跑 v2.15。请复核你的取证路径，`B 现役件`应以 `（62128716）…轨道B\python` 为准。

### 4) 12:52:59「重启疑云」——**排除，不是重启**

当天 `[INIT] track-B` **只出现 1 次**（行号 7，开盘启动）。中途无重载/重启 ⇒ 现象回到「打印策略静默」：`[LIM10] targets=` 全天仅 **6 次**（非每 bar），加上禁追高门的静默 `continue`。B 轨本日**无 bug** 结案。

### 5) 今日运行版本 + 今晚部署

- **今天实际运行**：A = **v2.46**、B = **v2.15**。⇒ 今日 A 的行为（`skip_low_abr` 误弃等）都是 v2.46 跑出来的；**early-vol(v2.47)/fixed-stop 关(v2.48)/abr 口径护栏(v2.49)、B 的 v2.16~v2.20 今天都还没生效**。
- **今晚已部署（磁盘层）**：A → **v2.49**（260,298B，21:39）、B → **v2.20**（218,174B，21:38）。
- **明日开盘**首条日志坐实：`[INIT] track-A … v2.49 … fixed-stop-OFF, abr-caliber-guard` / `[INIT] track-B … v2.20 …weak-regime+abr-caliber-guard…`；盘中留意 `abr=… src=feed win=800`。

### 6) 附：字节数 → 版本 反推标尺（供你核对现役件）

QMT 加密保存后 ≈ **明文 × 1.302**（4 份已知明文对齐，误差 <0.15%）：

| 位置 | 字节·时间 | ÷1.302 | 判定 |
|---|---|---|---|
| A 现役 `TRACK_A.py` | 260,298 · 09-16 21:39 | 199,922 | v2.49（明文 199,691）✅ |
| B 现役 `TRACK_B.py` | 218,174 · 09-16 21:38 | 167,568 | v2.20（167,552）✅ |
| A 互备件 `TRACK_B.py` | 202,650 · 09-16 14:58 | 155,645 | v2.16（155,652） |
| B 互备件 `TRACK_A.py` | 247,478 · 09-13 11:27 | 190,075 | v2.46（190,366） |

需要的话，请你在采集里把**真实现役件路径 + 字节 + 当日 `[INIT]` 串**三者并列，我们就能一眼判「内容变没变、版本串有没有随动」。

---

我这边已把上述定案写进 `knowledge/ops/qmt_client_dirs.md`（含标尺与日志定案）并推送；CHANGELOG/checkpoints 同步。**没有需要你返工的改动**，只需 note⑦ 口径与取证路径两处对齐。