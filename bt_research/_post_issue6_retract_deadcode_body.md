## @WB ⚠️ 更正：死代码那条**是我报错了** —— 问题只在本地冻结快照，**服务器版没有这个 bug**（Cursor 18:4x）

我上一条说 `trade_executor.py` 有"无条件 `continue` 致持仓静默蒸发"。**这条作废，我报错了**，原因值得记一笔。

### 一、为什么会错：**本地快照 ≠ 服务器**
我读的是**本地仓库根目录**的 `trade_executor.py`（Sep 5 快照），而运行时用的是**服务器** `/home/ubuntu/alphapilot/trade_executor.py`。两者 md5 不同，我把服务器版下载下来逐行 diff——**差异全部是缩进**：

| 行 | 本地快照（错） | **服务器（对）** |
|---|---|---|
| 203（v2） | `continue` 缩进 16 = 无条件 ⇒ 后续全死 | **缩进 20 = 在 `if pnl_pct<=stop_hard` 内** ⇒ 正常 fall-through |
| 326（非v2） | `continue` 缩进 12 = 无条件 ⇒ 后续全死 | **缩进 16 = 在 `if is_limit_up:` 内** ⇒ 趋势止盈 6% 可达 |

服务器版与 `trade_log` 行为一致（趋势止盈·峰值回撤 / 超期清理确实触发）⇒ **服务器逻辑正确，我误把一份被错误重排缩进的本地快照当成了生产代码。**

### 二、撤回清单
- ❌ "第326行无条件 `continue` ⇒ 正常持仓跳过 `remaining.append(p)`、静默蒸发" —— **不成立（服务器版 `remaining.append(p)` 在正常路径上）**；
- ❌ "v2 分支只有硬止损、达标激活/回撤1.5%不可达" —— **不成立**；
- ❌ "回归 bug" —— **不成立**。`trade_log` 反而证明服务器版一直正常。

**抱歉，这条浪费了你一轮。教训我记下了：报"生产代码 bug"前，必须先确认读的是服务器运行时文件，而不是仓库快照。**

### 三、但这轮审计出了**服务器版真实存在的**问题
用服务器版重审卖出流程，确认下列 3 条（都是真问题，非缩进）：

| # | 问题 | 影响 | 证据 |
|---|---|---|---|
| A | **`exit_policy` 元数据陈旧**：写 `ladder_tp=[[0.05,0.5],[0.10,1.0]]`，但常量注释明写 `LADDER_TP ... (已废除, 改用趋势跟踪峰值回撤6%)` | 你/我据此判断出场会得出错结论（你这次就读到了它）；`api_server` 另从 exit_policy 取 `trail_arm`/`peel_pullback`/`hard_stop_pct`，这三个 key **根本不存在** ⇒ 静默回落默认值 | `trade_executor.py:505-513` vs `:26` |
| B | **`CONSECUTIVE_MIN=3` 语义与排程不符**：注释说"−3% 需连续 **3 分钟**"，但 `trade_executor` 的 cron 是 **每 10 分钟一次**（:42/:52/:02/:12…）⇒ 实际要 **≈30 分钟**才减半 | 分级止损**远比设计迟钝**，弱市里少了一道保护 | `crontab` L112-116 |
| C | **涨停判定未区分板块**：`is_limit_up = pnl_pct >= 9.5`，对双创（20% 涨停）也按 9.5% 判定 | 双创股 +9.5% 未真涨停时会被当"封板"持有，跳过趋势止盈 | `trade_executor.py:321` |

另：`use_tp` / `t1_force_min` / `is_t1_window` / `_req` / `LADDER_TP` 均为死变量（无害，清理即可）。

### 四、另外一条**仓库卫生**问题（建议记卡）
> **根目录 `trade_executor.py` 的缩进与服务器不一致且是错误的**。任何人（包括我）读它都会推出错误结论。建议要么用服务器版覆盖、要么在文件头标明"非运行时副本，禁止据此判断逻辑"。

### 五、待老板拍板后我再动手
老板已让"出最小修复 + 回归单测 + 全流程审计"，但**修复靶子从"死代码"变成了 A/B/C**。我已把完整审计整理成文档，等确认修哪几条再改，**不擅自部署**。

—— Cursor
