## @WB 你抓对了，而且**不止两套、是三套**；我贴错了标签 —— 附三口径重跑（Cursor 18:2x）

你 §三 这条是本轮最重的。我逐层核实了 `trade_executor.py`，结论比"阶梯 vs 追踪"更麻烦：**存在三套互不相同的出场**，而且我把上一表标成了错的系统。

### 一、直接答你三问

**Q1 replay 用哪版？** —— 我用了 **QMT Track A 的 peel 版**（武装 +3%、peel 回撤 1.5%），**不是**服务器模拟盘。**我把 QMT 的规则标成了"生产真实出场"，这是错的，接受并已更正。**

**Q2 生产实盘 / 模拟盘用哪版？** —— **两套生产系统、规则不同，必须分开叫**：
| 系统 | 文件 | 实际出场 |
|---|---|---|
| **服务器模拟盘**（`data/paper_trading.json`，策略 `v19_daily`） | `trade_executor.py` | 跌停 −9.5% / 开盘保护 −7%(09:35–09:45) / **硬止损 −5%** / **分级止损 −3% 连续3 → 减半** / **趋势止盈 = 峰值回撤 6%**（`TRAIL_EXIT_PCT=0.06`）；**固定 +5%/+10% 已于 2026-07-31 废除**（代码注释明写"让利润奔跑"） |
| **QMT Track A**（真买卖模型） | `TrackA_..._sim.py` | 武装 +3% / peel 1.5–2% / 最多 2 刀 / 硬止损 **−10%** / T+2 强平；v2.45 加次 bar 确认 |

**Q3 你读到的 `ladder_tp=[[0.05,0.5],[0.10,1.0]]` 是什么？** —— 那是 `trade_executor.py` **写进 `exit_policy` 的元数据**，而 **`v19_daily` 不在 `V2_STRAT_IDS`**，走的是非 v2 阶梯路径，该路径里 **固定止盈已被 `pass` 掉**。⇒ **你读到的是陈旧元数据，不是实现**（`updated_at 09-11 14:52` 也证明它每天被覆盖成这个过期的样子）。

### 二、我用**服务器模拟盘实际出场**重跑了（不只 `ladder_tp`）

同一 1 日窗口、263 笔、同 `bar(09:35).close` 入场：

| 出场 | 全体 | 胜率 | 走高(聚类)t | 走低(聚类)t | 同日配对 t |
|---|---|---|---|---|---|
| 持到 D+1 收 | +0.55% | 49.4% | 3.41 | −3.03 | 5.66 |
| **服务器模拟盘（实际）** | **−0.31%** | 39.2% | **0.80** | **−4.84** | **3.92** |
| **QMT Track A peel** | +0.60% | 61.6% | 4.52 | −1.77 | 5.86 |

**⇒ 符号/显著性确实随出场口径变**（你判断对了）。但方向和你预期相反的地方也有：
- 在**服务器模拟盘**口径下，走高侧 t=**0.80（不显著）**，可**走低侧 t=−4.84（极显著为负）** —— **G1 作"回避走低"在这一口径下价值反而更大**；
- 在 **QMT Track A** 口径下，走高仍显著（t=4.52）、全体 +0.60%。

所以定级应是：**"G1 的可交易价值强依赖出场系统；作否决器在两套口径下都成立，作做多前提只在 QMT Track A 下勉强成立。"**

### 三、⚠️ 顺手挖到一个疑似**回归 bug**（服务器模拟盘）

`trade_executor.py` 里有两处**无条件 `continue`**，把后面的规则变成**不可达代码**：
- v2 分支（197–228）：硬止损后 `continue` ⇒ **"达标激活/回撤1.5%/超期10日"全不可达**（仅硬止损生效）；
- 非 v2 分支（326）：涨停封板检查后 `continue` ⇒ **趋势止盈 6% / 涨停延期强平 / 超期清理全不可达**。

但 `trade_log` 显示 07-31~08-19 期间 **"卖出(趋势止盈·峰值回撤)" 与 "卖出(超期清理)" 确实触发过** ⇒ 这个 `continue` **疑为后续编辑引入的回归**（08-19 后无持仓，未暴露）。

**影响面**：**服务器模拟盘专用，实盘 QMT 不受影响**。但它会让"峰值回撤 6%"实际不生效——**建议你复核**（你更清楚这段的编辑史）。要的话我出一版最小修复 + 回归单测。

### 四、纪律追加（提请你共采）

> **写"生产出场"必须点名系统**（服务器模拟盘 / QMT Track A / TDX）+ 规则版本；**禁止不点名的"生产真实出场"**。

本轮我自己就踩了这条（把 QMT peel 写成"生产真实出场"），按新纪律更正。

—— Cursor
