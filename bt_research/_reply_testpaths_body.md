## 【补办 · 你同意的两项】行尾回归已修 + 20 个测试路径改可移植（Cursor 09-12 16:2x）

### 一、行尾回归（你抓的那条）**已修并推送**
- 4 个文件从 LF **还原为原始 CRLF**：`sim_v2.13`(3116)、`sim_v2.12`(3085)、`track_a sim_v2.45`(3880)、`checkpoints.md`(388)。
- `sim_v2.13` 去掉行尾后**只剩 14 行真改动**（全是注释里的文件名引用）——"纯改名"已名副其实。
- `README.md` 加了「行尾约定」：QMT/TDX `.py` 保 CRLF、文档保 LF，**禁止整文件重写行尾**；改动后核对 `git diff --stat` 量级。
- 提交 `4389153`（amend，`977e118` 作废）+ 文档 `2c3d33c`，均已推 origin/master。

### 二、`a972e0a5…` 复现不出的解释（不是矛盾）
- `a972e0a5…` = **改名前**的 CRLF 文件（`32336cd:` 旧 `…_sim.py`）——我复现了，值正确。
- `3d76848a…` = 你手工还原的 CRLF 版 = **现在的工作区/HEAD 值**。
- 两者之差 = 改名的 14 行注释内文件名引用。**没有第三个值**；你那个 `0fcb2a16…`（LF）是修前中间态。

**新权威 md5（CRLF）**：`TrackB_…_sim_v2.13.py` = `3d76848a22f714bb0de87084cde4ba18`；`TrackA_…_sim_v2.45.py` = `4d43f181cc804d597723c0df9cf62fec`。
⚠️ **行尾声明**：以上为 CRLF。现场若拿到 LF 版 md5 会不同——**先看 `\r\n` 是否存在再下"不匹配"结论**。

### 三、Fix A 未被回滚（先核先答）
`_sim_v2.13.py` 内 `money_pass all rejected` **2 处仍在**、空仓短路 `... and not money_items` 在（L2833）；`_sim_v2.12.py` 备份内 Fix A 标记 **0 处**（没泄漏）。

### 四、flat-log —— **按老板拍板：暂不恢复，随 Fix C（v2.14）打包**
理由：老板当前部署件不含它；现在恢复会让仓库又比部署件新一版，**重新引入"仓库≠部署"**。Fix C 必然升 v2.14 + 重新部署，届时一次带上。**不是否决这条改进。**

### 五、20 个测试文件的绝对路径 —— **已改（`732dfdb`）**
`production_strategies/` 下 20 个 `_test_*`/分析/回测脚本写死 `C:\Users\elvisq\Projects\alphapilot`，Mac 上 `FileNotFoundError`。改为顶部按 `__file__` 推 `_REPO` + `str(_REPO / ...)`（注入点避开 `from __future__`）；**行尾不变**（原均 LF）；**20/20 编译通过**；**未动任何生产策略文件**。

**修后测试能跑了，但暴露既有红（非本次引入）**：`_test_max_cand_rank` 断言 `v2.30-tpl/v2.30/v2.29`（现 `v2.38-tpl/v2.45/v2.30`）、`_test_buy_window` 4 项、`_test_sell_dynamic_v16` 2 项 —— 都是**版本期望陈旧**。
**建议**：改期望版本号那类是机械的，可另单做；**动断言前请老板/你确认**"期望旧版"是历史遗留还是真回归。要不要做，等你点名。

### 六、范围说明
`scripts/` + `bt_research/` 下另有约 **52 个**同类硬编码路径，**本次未动**（按你确认的口径只改 `production_strategies/` 的 20 个）。要不要一并改，另议。

—— Cursor
