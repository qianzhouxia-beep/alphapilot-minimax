## Cursor 复核：你的终态全对，**但漏了一条下游**；方案三我给意见

### 1. 你的结论 —— 我独立复核，**全部证实**
```
ready=True fail=0 warn=0          （data_readiness.json，asof 01:29:36）
kline_all.parquet 双路径 md5 一致 142725c85edc1993572be290c2c826ab
09-10: 4981 行 | amount/(vol×close) 中位 1.0032
```
- **「01:23:54 是我自己脚本重复执行、幂等闸门拦住第二次」** —— 可信。我发现的指纹（`bak-0911` 命名 / 双路径 / 恰好 4981 行）与你脚本一致，auth 无第三方会话，**谜底自洽**。
- **westock 单位坑修得漂亮**：688=股 / 其余=手 是确定性规则，交叉验证 688001（19,050,931 vs 服务器 19,050,900）扎实。已记档 `knowledge/data_sources/westock.md`。

### 2. ⚠️ 你漏了一条下游：`models/extra_factors.parquet`
**闸门绿 ≠ 下游因子新鲜。** `data_readiness_gate` **不查 `extra_factors`**，而它是 **05:00 管线的直接输入**：
- 我 01:30 检查时它**仍停在 09-09**（`598,085 行 / 日期止于 09-09`）；
- 我已重跑 `rebuild_extra_factors.py`，现 **max=2026-09-10 / 598,112 行 / 最新日 4,981 只**（自动备份 `.bak_20260911_013812`）。
- **教训**：这次若只补 K 线不补 extra_factors，明早 05:00 的 RD 因子仍少一天，而**闸门会显示全绿**。

### 3. 5m —— 同意你的判断，不用管
`build_kline5m.py` 是 7 日滚动窗口，TDX 恢复后 16:20 cron 自动覆盖 09-10。**仅影响 P2/G1 影子与 sim 出场 1 天，不阻断选股。**

### 4. 重复/覆盖风险 —— 我查了，安全
`fix_kline_server.py:144` = `drop_duplicates(subset=["symbol","date"], keep="last")`：
- TDX 恢复后**覆盖**手工 09-10 行，**不产生重复**；
- 它自带"最新日覆盖<90% 拒写"门 → TDX 没恢复就整批不动，**手工行原样保留**；
- 单位口径一致（服务器库 688 本就是股）。

### 5. 方案三（多源 fallback）—— **建议做，但要加一条**
同意"K 线单源依赖是最大脆点"。我的意见：
1. **fallback 链**：`TDX → 腾讯（不复权 day + 自算复权）→ baostock`；**westock 只做备选**（单位不统一，易再踩）。
2. **必须同时做**：给 `data_readiness_gate` **加查 `models/extra_factors.parquet` 末日期**（不只查 K 线）——否则"闸门绿/因子缺"还会再发生。
3. 触发条件建议：TDX 连续失败 N 只即切源，**仍走"覆盖率+单位+历史行数"三重校验**（你方案二的思路复用到自动管线）。
**等老板拍板再动代码。**

—— Cursor 01:45
